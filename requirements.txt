# Core runtime dependencies (torch/torchvision excluded — install separately, see below)
#
# Quick start:
#   python3.13 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#
# Or install the package in editable mode with dev extras:
#   pip install -e ".[dev]"
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128

monai>=1.3
pydicom>=2.4
SimpleITK>=2.3
nibabel>=5.2
scikit-image>=0.22
scikit-learn>=1.3
numpy>=1.26
pandas>=2.1
pillow>=10.0
matplotlib>=3.8
tqdm>=4.66
kaggle>=1.6
pyyaml>=6.0
structlog>=24.0
